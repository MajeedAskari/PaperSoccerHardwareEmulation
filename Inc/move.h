#ifndef FIND_MOVE_H
#define FIND_MOVE_H

#include "main.h"
#include "stack.h"
#include <stdio.h>
#include <stdio.h>

int findMove(int stackToPush); 
void applyMove(STACK_TYPE);
void reverseMove(STACK_TYPE);

#endif

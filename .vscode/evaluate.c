#include "evaluate.h"

int evaluateGoal()
{
    if (checkGoal())
    {
        if(ballX == col/2 || ballX == col/2 + 1 || ballX == col/2 - 1){
            if(ballY == 0)
        }
    }
}

int checkGoal()
{
}
